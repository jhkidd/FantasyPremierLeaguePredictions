# Brainstorming session artefacts
.superpowers/

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# Tooling caches
.pytest_cache/
.ruff_cache/

# Scratch output from `fpl features` and ad-hoc exploration.
# Never inside data/ — data/ is the committed record.
scratch/

# Notebook outputs are stripped before commit
.ipynb_checkpoints/

# Training matrices are a pure function of facts/features (data/eda/ figures
# stay tracked; the matrix itself is large and trivially rebuilt via
# `fpl dataset`, so it is the one derived artefact excepted from the
# data/-is-the-committed-record rule above).
data/training/
