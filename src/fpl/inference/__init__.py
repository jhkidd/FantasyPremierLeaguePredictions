"""Inference: the production entrypoint for turning a frozen model
registry (:mod:`fpl.training.registry`) into predictions for the upcoming
gameweek (Phase C, `.github/context/subsystem3-close-and-mvp-site.md`)."""

from __future__ import annotations
