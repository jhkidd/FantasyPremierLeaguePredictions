"""Model-prototype training pipeline: dataset assembly, splits, EDA, baseline.

Phase A (`.github/context/model-prototype-phase-9.md`). Nothing in this
package is the source of truth — `data/training/` is gitignored and always
rebuildable from `facts/` and `features/`.
"""

from __future__ import annotations
